# egis-agent-studio 是"被宿主进程挂载的模块"，不自带独立 .env。
#
# 它的环境变量由宿主业务项目（如 egis-gpt-agents/.env）统一提供：
#   - AGENTS_DIR / CORE_SKILLS_DIR / STUDIO_DATA_DIR / DB_*  业务项目 .env
#   - ENABLE_EGIS_STUDIO=true 打开 create_app() 的 Studio 挂载开关
#
# 启动方式（零代码挂载）：
#   cd egis-gpt-agents
#   uv sync --all-extras
#   uv run egis-gpt-agents
#   # → :38081 同时承载业务路由 + Studio 路由
